import type { NextConfig } from "next";


const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, // Disable the "Next.js is running in development mode" indicator

};

export default nextConfig;
